namespace Ludo.Constants
{
    public static class PlayerConstants
    {
        // Maximum players that a game can have 
        public const int MaxPlayers = 4;
        // Pawns per each player 
        public const int PawnsPerPlayer = 4;
        // Init pawns at home for each player 
        public const int InitPawnsAtHome = 4;
        // Init pawns at the escape position for each player 
        public const int InitPawnsEscaped = 0;
    }
}